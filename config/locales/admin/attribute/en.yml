en:
  attributes: attributes 
  attribute: 
    one: attribute
    other: attributes
    create: 
      text: create an attribute
      choice: choose an attribute
      access_method: attribute
    value:
    all: all attributes
    update:
      action: update attribute
    back:
    duplicate:
      action: duplicate attribute
    destroy:
      action: delete attribute
    access_method: access method

  text_attribute:

  longtext_attribute:

  url_attribute:

  number_attribute:

  date_attribute:

  picklist_attribute:

  checkbox_attribute:
     field:

  radiobutton_attribute:
