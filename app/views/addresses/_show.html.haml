%pre
  \#{t(address.civility, :scope => [:civility, :label])} &nbsp; #{address.name} &nbsp; #{address.firstname}

  \#{address.address} &nbsp; #{address.address_2}
  
  \#{address.zip_code} &nbsp; #{address.city} &nbsp; #{address.country.name if address.country}
