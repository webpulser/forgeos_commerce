%h1
  Special offer
- content_for :tools, link_to('Create a special offer', new_admin_special_offer_path, :class => 'add')
#special_offers_list
  %table#table
    %thead
      %tr
        %th
          Name
        %th
          Description
        %th
          = I18n.t('action', :count => 2).capitalize
    %tbody
      - @special_offers.each do |s|
        %tr
          %td
            = s.name
          %td
            = s.description
          %td
            = link_to I18n.t('edit').capitalize, edit_admin_special_offer_path(s)
            \-
            = link_to I18n.t('destroy').capitalize, admin_special_offer_path(s), :method => 'delete', :confirm => 'Delete this speciam offer ?'

= dataTables_tag(3)
