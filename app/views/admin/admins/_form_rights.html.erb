<%= hidden_field_tag "admin[right_ids][]" %>
<table>
<% rights.in_groups_of(4,false).each do |right_group| %>
  <tr>
    <% right_group.each do |right| %> 
    <td class='content'>
    <% if admin %>
      <%= check_box_tag "admin[right_ids][]", right.id, admin.rights.include?(right) %><%= right.name %>
    <% else %>
      <%= check_box_tag "admin[right_ids][]", right.id, false %><%= right.name %>
    <% end %>
    </td>
    <% end %>
  </tr>            
<% end %>
</table>
