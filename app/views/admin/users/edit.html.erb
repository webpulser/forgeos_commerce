<h1>Edit User</h1>
<%= render :partial => 'form', :locals => { :user => @user } %>
<% unless @user.active? %>
  <%= @content_for_tools << link_to('activate', :controller => 'users', :action => 'activate', :id => @user.id ) %>
<% end %>
