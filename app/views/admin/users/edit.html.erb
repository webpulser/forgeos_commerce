<h1><%= I18n.t('edit_user').humanize %></h1>
<%= render :partial => 'form', :locals => { :user => @user } %>
<% unless @user.active? %>
  <%= @content_for_tools << link_to(I18n.t('activate').humanize, :controller => 'users', :action => 'activate', :id => @user.id ) %>
<% end %>
